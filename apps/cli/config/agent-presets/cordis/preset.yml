name: Creator
description: Standard capabilities plus the ability to read and modify the harness runtime: author custom agent presets, inspect the live composition, and experiment with plugins.
order: 4
